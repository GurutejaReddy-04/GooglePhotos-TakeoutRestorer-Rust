import { Theme } from "Theme.slint";

export component WindowChrome inherits Window {
    title: "Google Photos Takeout Restorer";
    background: Theme.bg_app;
    no-frame: false;
    default-font-family: Theme.font_family;
    
    @children
}
